/**
 * @readonly
 * @enum {String}
 */
module.exports = Object.freeze({
	/**
	 * Link to a website with player.
	 * datatype: string
	 */
	EMBED: "embed",
	/**
	 * Link directly to the video file.
	 * datatype: string
	 */
	DIRECT: "direct",
	/**
	 * Base64 encoded string, aes encrypted.
	 * Use util.kissCrypto to decrypt.
	 * datatype: string
	 */
	OVELWRAP: "ovelwrap",
	/**
	 * Link to a HLS stream.
	 * datatype: string
	 */
	HLS: "hls",
});
