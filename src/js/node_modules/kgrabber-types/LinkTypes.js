/**
 * @readonly
 * @enum {String}
 */
module.exports = Object.freeze({
	/**
	 * Points to a website with player;
	 * datatype: string
	 */
	EMBED: "embed",
	/**
	 * Points directly to the video file;
	 * datatype: string
	 */
	DIRECT: "direct",
});
